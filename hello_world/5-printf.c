#include <stdio.h>
/**
 * main - Entry Point
 *
 * Description: Prints a message to stdout using printf.
 * Return: Always 0 if Success.
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
