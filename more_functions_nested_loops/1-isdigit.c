#include "main.h"
/**
 * _isdigit - Function that check for digit from 0 to 9.
 * @c: The variable that store the character to check.
 * Return: 1 if c is digit, 0 otherwise.
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	{
		return (0);
	}
}
