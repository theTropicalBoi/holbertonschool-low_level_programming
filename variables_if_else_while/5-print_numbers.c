#include <stdio.h>
/**
 * main - Entry Point
 *
 * Description: Print all single digit of base 10 from 0.
 * Return: Always 0 if succeed.
 */
int main(void)
{
int n;
for (n = 0; n < 10; n++)
printf("%d", n);
printf("\n");

return (0);
}
