#include "main.h"
/**
 * _abs - Function that computes the absolute value of an integer.
 * Description: Function that print absolute value.
 * @r: Initial value to be computes.
 * Return: Always 0
*/
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
