#include "main.h"
/**

 * _abs: Computes the absolute value of an integer.
 * @int: Initial value to be computes.
 * Return: Always 0
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	} else
	{
		return (-r);
	}
}
